apiVersion: apps/v1
kind: Deployment

metadata:
  name: pulsewatch-backend

spec:
  replicas: 1

  selector:
    matchLabels:
      app: pulsewatch-backend

  template:
    metadata:
      labels:
        app: pulsewatch-backend

    spec:
      containers:
        - name: backend
          image: pragg3/pulsewatch-backend
          imagePullPolicy: Always

          ports:
            - containerPort: 8080

          envFrom:
            - configMapRef:
                name: pulsewatch-backend-config
            - configMapRef:
                name: pulsewatch-installation-config
                optional: true

          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: DATABASE_URL

            - name: NETWORK_DISCOVERY_KEY
              valueFrom:
                secretKeyRef:
                  name: pulsewatch-security-secret
                  key: NETWORK_DISCOVERY_KEY

          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 5

          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10